

# Plano: Aumentar tamanho do certificado Cadastur

O certificado está com `max-w-sm` (384px). Vou aumentar para `max-w-md` (448px) e expandir o container geral de `max-w-4xl` para `max-w-5xl` para dar mais espaço à imagem.

## Arquivo a editar
| Arquivo | Mudança |
|---------|---------|
| `src/pages/Sobre.tsx` | Linha 150: `max-w-4xl` → `max-w-5xl`. Linha 162: `max-w-sm` → `max-w-md`. Imagem do certificado fica maior e com mais espaço. |

